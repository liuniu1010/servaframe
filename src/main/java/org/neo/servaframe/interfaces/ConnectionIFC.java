package org.neo.servaframe.interfaces;

/***
 * the interface which encapsulate connection for persist like Database
 * and provide methods to visit third persist lib in its subclass
 *
 */
public interface ConnectionIFC {
}
